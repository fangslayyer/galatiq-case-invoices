# xAI API key — enables the real Grok backend (otherwise the offline stub runs)
XAI_API_KEY=

# Optional overrides (defaults shown)
# INVOICEFLOW_LLM_BACKEND=auto        # grok | stub | auto
# INVOICEFLOW_GROK_MODEL=grok-3
# INVOICEFLOW_SCRUTINY_THRESHOLD=10000
